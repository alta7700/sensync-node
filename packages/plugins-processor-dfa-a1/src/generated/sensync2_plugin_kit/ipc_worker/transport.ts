// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               v6.33.2
// source: sensync2_plugin_kit/ipc_worker/transport.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "sensync2_plugin_kit.ipc_worker";

export interface Ready {
  protocolVersion: number;
  workerVersion: string;
  methods: string[];
}

export interface Request {
  requestId: string;
  method: string;
  payload: Uint8Array;
}

export interface WorkerError {
  code: string;
  message: string;
}

export interface Response {
  requestId: string;
  ok: boolean;
  payload: Uint8Array;
  error?: WorkerError | undefined;
}

export interface Shutdown {
}

export interface Envelope {
  payload?:
    | { $case: "ready"; ready: Ready }
    | { $case: "request"; request: Request }
    | { $case: "response"; response: Response }
    | { $case: "shutdown"; shutdown: Shutdown }
    | undefined;
}

function createBaseReady(): Ready {
  return { protocolVersion: 0, workerVersion: "", methods: [] };
}

export const Ready: MessageFns<Ready> = {
  encode(message: Ready, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.protocolVersion !== 0) {
      writer.uint32(8).uint32(message.protocolVersion);
    }
    if (message.workerVersion !== "") {
      writer.uint32(18).string(message.workerVersion);
    }
    for (const v of message.methods) {
      writer.uint32(26).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Ready {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReady();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.protocolVersion = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.workerVersion = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.methods.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Ready {
    return {
      protocolVersion: isSet(object.protocolVersion)
        ? globalThis.Number(object.protocolVersion)
        : isSet(object.protocol_version)
        ? globalThis.Number(object.protocol_version)
        : 0,
      workerVersion: isSet(object.workerVersion)
        ? globalThis.String(object.workerVersion)
        : isSet(object.worker_version)
        ? globalThis.String(object.worker_version)
        : "",
      methods: globalThis.Array.isArray(object?.methods) ? object.methods.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: Ready): unknown {
    const obj: any = {};
    if (message.protocolVersion !== 0) {
      obj.protocolVersion = Math.round(message.protocolVersion);
    }
    if (message.workerVersion !== "") {
      obj.workerVersion = message.workerVersion;
    }
    if (message.methods?.length) {
      obj.methods = message.methods;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Ready>, I>>(base?: I): Ready {
    return Ready.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Ready>, I>>(object: I): Ready {
    const message = createBaseReady();
    message.protocolVersion = object.protocolVersion ?? 0;
    message.workerVersion = object.workerVersion ?? "";
    message.methods = object.methods?.map((e) => e) || [];
    return message;
  },
};

function createBaseRequest(): Request {
  return { requestId: "", method: "", payload: new Uint8Array(0) };
}

export const Request: MessageFns<Request> = {
  encode(message: Request, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requestId !== "") {
      writer.uint32(10).string(message.requestId);
    }
    if (message.method !== "") {
      writer.uint32(18).string(message.method);
    }
    if (message.payload.length !== 0) {
      writer.uint32(26).bytes(message.payload);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Request {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.method = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.payload = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Request {
    return {
      requestId: isSet(object.requestId)
        ? globalThis.String(object.requestId)
        : isSet(object.request_id)
        ? globalThis.String(object.request_id)
        : "",
      method: isSet(object.method) ? globalThis.String(object.method) : "",
      payload: isSet(object.payload) ? bytesFromBase64(object.payload) : new Uint8Array(0),
    };
  },

  toJSON(message: Request): unknown {
    const obj: any = {};
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    if (message.method !== "") {
      obj.method = message.method;
    }
    if (message.payload.length !== 0) {
      obj.payload = base64FromBytes(message.payload);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Request>, I>>(base?: I): Request {
    return Request.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Request>, I>>(object: I): Request {
    const message = createBaseRequest();
    message.requestId = object.requestId ?? "";
    message.method = object.method ?? "";
    message.payload = object.payload ?? new Uint8Array(0);
    return message;
  },
};

function createBaseWorkerError(): WorkerError {
  return { code: "", message: "" };
}

export const WorkerError: MessageFns<WorkerError> = {
  encode(message: WorkerError, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.code !== "") {
      writer.uint32(10).string(message.code);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkerError {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkerError();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.code = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WorkerError {
    return {
      code: isSet(object.code) ? globalThis.String(object.code) : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: WorkerError): unknown {
    const obj: any = {};
    if (message.code !== "") {
      obj.code = message.code;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WorkerError>, I>>(base?: I): WorkerError {
    return WorkerError.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WorkerError>, I>>(object: I): WorkerError {
    const message = createBaseWorkerError();
    message.code = object.code ?? "";
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseResponse(): Response {
  return { requestId: "", ok: false, payload: new Uint8Array(0), error: undefined };
}

export const Response: MessageFns<Response> = {
  encode(message: Response, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requestId !== "") {
      writer.uint32(10).string(message.requestId);
    }
    if (message.ok !== false) {
      writer.uint32(16).bool(message.ok);
    }
    if (message.payload.length !== 0) {
      writer.uint32(26).bytes(message.payload);
    }
    if (message.error !== undefined) {
      WorkerError.encode(message.error, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Response {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.ok = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.payload = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.error = WorkerError.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Response {
    return {
      requestId: isSet(object.requestId)
        ? globalThis.String(object.requestId)
        : isSet(object.request_id)
        ? globalThis.String(object.request_id)
        : "",
      ok: isSet(object.ok) ? globalThis.Boolean(object.ok) : false,
      payload: isSet(object.payload) ? bytesFromBase64(object.payload) : new Uint8Array(0),
      error: isSet(object.error) ? WorkerError.fromJSON(object.error) : undefined,
    };
  },

  toJSON(message: Response): unknown {
    const obj: any = {};
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    if (message.ok !== false) {
      obj.ok = message.ok;
    }
    if (message.payload.length !== 0) {
      obj.payload = base64FromBytes(message.payload);
    }
    if (message.error !== undefined) {
      obj.error = WorkerError.toJSON(message.error);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Response>, I>>(base?: I): Response {
    return Response.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Response>, I>>(object: I): Response {
    const message = createBaseResponse();
    message.requestId = object.requestId ?? "";
    message.ok = object.ok ?? false;
    message.payload = object.payload ?? new Uint8Array(0);
    message.error = (object.error !== undefined && object.error !== null)
      ? WorkerError.fromPartial(object.error)
      : undefined;
    return message;
  },
};

function createBaseShutdown(): Shutdown {
  return {};
}

export const Shutdown: MessageFns<Shutdown> = {
  encode(_: Shutdown, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Shutdown {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseShutdown();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): Shutdown {
    return {};
  },

  toJSON(_: Shutdown): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<Shutdown>, I>>(base?: I): Shutdown {
    return Shutdown.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Shutdown>, I>>(_: I): Shutdown {
    const message = createBaseShutdown();
    return message;
  },
};

function createBaseEnvelope(): Envelope {
  return { payload: undefined };
}

export const Envelope: MessageFns<Envelope> = {
  encode(message: Envelope, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.payload?.$case) {
      case "ready":
        Ready.encode(message.payload.ready, writer.uint32(10).fork()).join();
        break;
      case "request":
        Request.encode(message.payload.request, writer.uint32(18).fork()).join();
        break;
      case "response":
        Response.encode(message.payload.response, writer.uint32(26).fork()).join();
        break;
      case "shutdown":
        Shutdown.encode(message.payload.shutdown, writer.uint32(34).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Envelope {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEnvelope();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.payload = { $case: "ready", ready: Ready.decode(reader, reader.uint32()) };
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.payload = { $case: "request", request: Request.decode(reader, reader.uint32()) };
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.payload = { $case: "response", response: Response.decode(reader, reader.uint32()) };
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.payload = { $case: "shutdown", shutdown: Shutdown.decode(reader, reader.uint32()) };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Envelope {
    return {
      payload: isSet(object.ready)
        ? { $case: "ready", ready: Ready.fromJSON(object.ready) }
        : isSet(object.request)
        ? { $case: "request", request: Request.fromJSON(object.request) }
        : isSet(object.response)
        ? { $case: "response", response: Response.fromJSON(object.response) }
        : isSet(object.shutdown)
        ? { $case: "shutdown", shutdown: Shutdown.fromJSON(object.shutdown) }
        : undefined,
    };
  },

  toJSON(message: Envelope): unknown {
    const obj: any = {};
    if (message.payload?.$case === "ready") {
      obj.ready = Ready.toJSON(message.payload.ready);
    } else if (message.payload?.$case === "request") {
      obj.request = Request.toJSON(message.payload.request);
    } else if (message.payload?.$case === "response") {
      obj.response = Response.toJSON(message.payload.response);
    } else if (message.payload?.$case === "shutdown") {
      obj.shutdown = Shutdown.toJSON(message.payload.shutdown);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Envelope>, I>>(base?: I): Envelope {
    return Envelope.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Envelope>, I>>(object: I): Envelope {
    const message = createBaseEnvelope();
    switch (object.payload?.$case) {
      case "ready": {
        if (object.payload?.ready !== undefined && object.payload?.ready !== null) {
          message.payload = { $case: "ready", ready: Ready.fromPartial(object.payload.ready) };
        }
        break;
      }
      case "request": {
        if (object.payload?.request !== undefined && object.payload?.request !== null) {
          message.payload = { $case: "request", request: Request.fromPartial(object.payload.request) };
        }
        break;
      }
      case "response": {
        if (object.payload?.response !== undefined && object.payload?.response !== null) {
          message.payload = { $case: "response", response: Response.fromPartial(object.payload.response) };
        }
        break;
      }
      case "shutdown": {
        if (object.payload?.shutdown !== undefined && object.payload?.shutdown !== null) {
          message.payload = { $case: "shutdown", shutdown: Shutdown.fromPartial(object.payload.shutdown) };
        }
        break;
      }
    }
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
