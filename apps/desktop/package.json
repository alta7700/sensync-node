{
  "name": "@sensync2/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wait-on http-get://localhost:5173 && NODE_OPTIONS='--import=tsx' electron --no-sandbox ./src/main.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@sensync2/core": "0.1.0",
    "@sensync2/runtime": "0.1.0"
  }
}
